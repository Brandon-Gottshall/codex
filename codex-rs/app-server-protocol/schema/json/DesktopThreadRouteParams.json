{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "focus": {
      "description": "When true, the desktop host may focus/raise its window while routing. Omission means false so clients can request quiet routing by default.",
      "type": "boolean"
    },
    "threadId": {
      "type": "string"
    }
  },
  "required": [
    "threadId"
  ],
  "title": "DesktopThreadRouteParams",
  "type": "object"
}